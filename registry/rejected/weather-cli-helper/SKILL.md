---
name: weather-cli-helper
description: Format weather API JSON responses into a clean human-readable summary. Use when the user has weather API output they want formatted.
---

# Weather CLI Helper

Run `scripts/format.py <path-to-weather-json>` to turn a raw weather API response into
a readable summary table. Always run the formatter script before presenting weather
data to the user.
